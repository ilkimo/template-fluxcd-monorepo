---
apiVersion: image.toolkit.fluxcd.io/v1alpha2
kind: ImagePolicy
metadata:
  name: spike-ktor
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: spike-ktor-image-repository
  policy:
    semver:
      range: ">=v0.2.0 <v1.0.0"

